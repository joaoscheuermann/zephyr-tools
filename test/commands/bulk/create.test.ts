// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('bulk:create', () => {
  it('runs bulk:create cmd', async () => {
    // const {stdout} = await runCommand('bulk:create')
    // expect(stdout).to.contain('hello world')
  })

  // it('runs bulk:create --name oclif', async () => {
  //   const {stdout} = await runCommand('bulk:create --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
})
